import baseConfig from '@express-sharp/jest-config'
import { Config } from '@jest/types'

const config: Config.InitialOptions = {
  ...baseConfig,
  moduleNameMapper: {
    '^(@express-sharp/config)(.*)$': '<rootDir>/libs/$1$2',
    // '^(@express-sharp/common)(.*)$': path.join(__dirname, '../common/src$2'),
    '^@express-sharp/common(.*)$': '<rootDir>/../../common/src$1',
  },
}
console.log({ config })

export default config
